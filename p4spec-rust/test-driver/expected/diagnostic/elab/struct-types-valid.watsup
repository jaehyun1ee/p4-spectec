syntax record = { COUNT nat, FLAG bool }
dec $make : record
def $make = { COUNT 1, FLAG true }
dec $get(record) : nat
def $get(record) = record.COUNT
dec $set(record) : record
def $set(record) = record[.COUNT = 2]
