error[elab/premise-otherwise-iteration-unsupported]: cannot iterate an `otherwise` premise
  ┌─ elab/relation-iter-otherwise-premise.watsup:8:7
  │
8 │   -- (otherwise)*
  │       ^^^^^^^^^
  │
  = An `otherwise` premise selects one fallback path for the rule.

