error[algo/expression-variable-unbound]: `x` is not bound when this expression is evaluated
  ┌─ algo/dataflow-read-unbound-output.watsup:9:8
  │
9 │   0 |- x + 1
  │        ^ `x` is read here
  │
  = Bind the variable in an earlier input pattern or premise before reading it here.

