error[algo/relation-input-variable-unbound]: `x` is not bound when this expression is evaluated
   ┌─ algo/dataflow-read-unbound-input.watsup:10:9
   │
 6 │   hint(input %0)
   │              -- argument %0 of relation `R` is evaluated as input; it cannot bind `x`
   ·
10 │   -- R: x + 1 |- 0
   │         ^ `x` is read here
   │
   = Bind the variable in an earlier input pattern or premise before reading it here.

