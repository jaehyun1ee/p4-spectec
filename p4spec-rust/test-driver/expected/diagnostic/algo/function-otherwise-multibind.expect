error[algo/otherwise-multibind-invalid]: repeating `x` requires an equality check, which is not allowed in an `otherwise` clause
  ┌─ algo/function-otherwise-multibind.watsup:5:11
  │
5 │ def $f(x, x) = x
  │        -  ^ repeated `x`
  │        │   
  │        first bound here
6 │   -- otherwise
  │      --------- `otherwise` clause starts here

