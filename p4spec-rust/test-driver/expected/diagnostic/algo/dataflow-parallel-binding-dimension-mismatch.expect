error[algo/binding-dimension-mismatch]: parallel bindings for `x` have incompatible dimensions: `foo` and `foo*`
   ┌─ algo/dataflow-parallel-binding-dimension-mismatch.watsup:10:8
   │
10 │   (x, (x, y)*) |- true
   │    -   ^
   │    │    
   │    first bound here
   │
   = A variable can have only one type and iteration dimension within a binder pattern. These two positions give the variable different dimensions.

