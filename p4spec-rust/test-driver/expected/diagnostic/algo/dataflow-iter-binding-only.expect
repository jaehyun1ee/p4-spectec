error[algo/iteration-loop-variable-missing]: iteration has no iter variable because `x` is newly bound
   ┌─ algo/dataflow-iter-binding-only.watsup:13:7
   │
13 │   -- (if x = 0)*
   │       ^^^^^^^^
   │
   = An iteration needs a variable bound outside it whose values it can iterate over.

