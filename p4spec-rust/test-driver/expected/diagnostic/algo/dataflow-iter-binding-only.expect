error[algo/iteration-loop-variable-missing]: this iteration has no previously bound variable to determine its range
   ┌─ algo/dataflow-iter-binding-only.watsup:13:6
   │
13 │   -- (if x = 0)*
   │      ^^^^^^^^^^^
   │      │   │
   │      │   `x` is newly bound here
   │      this iteration has no iteration source
   │
   = A variable bound outside the iteration must supply the values it ranges over.

