error[algo/iteration-loop-variable-missing]: this iteration has no previously bound variable to determine its length
   ┌─ algo/dataflow-iter-binding-only.watsup:13:6
   │
13 │   -- (if x = 0)*
   │      ^^^^^^^^^^^
   │      │   │
   │      │   `x` is newly bound here
   │      this iteration has no iteration source
   │
   = Bind a variable before this iteration so its values determine the iteration length.

