error[algo/expression-variable-unbound]: `u` is not bound when this expression is evaluated
   ┌─ algo/dataflow-free-variable-in-output.watsup:14:8
   │
14 │   v |- u
   │        ^ `u` is read here
   │
   = Bind the variable in an earlier input pattern or premise before reading it here.

