error[parse/token-invalid]: unexpected token "}"
  ┌─ parse/parse-stray-rbrace.watsup:3:1
  │
3 │ }
  │ ^ expected a line break (a blank line, two blank lines) or a keyword (such as `syntax`, `extern`, `tbl`) or end of input

